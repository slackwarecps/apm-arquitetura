# CLAUDE.md
<!-- Generated by APM CLI -->
<!-- Build ID: apm-arquitetura-0.2.0 -->

## Project Standards

### Files matching `**/*`

# Diretrizes de Arquitetura - Fabao-Corp

1. Backends e BFFs sao Java Spring Boot; frontend web e Angular; mobile e Android/iOS nativos.
2. Comunicacao sincrona (REST/gRPC) so quando necessario; preferir eventos para fluxos assincronos entre squads/microsservicos.
3. Contratos de API: versionamento explicito, compatibilidade retroativa, OpenAPI/AsyncAPI publicado.
4. Resiliencia: timeout, retry com backoff, circuit breaker em chamadas entre servicos (Spring Boot: Resilience4j).
5. Observabilidade: logs estruturados, tracing distribuido, metricas minimas (latencia, erro, saturacao) em todo novo endpoint.
6. Front/Mobile consomem sempre o BFF, nunca o backend diretamente; tratamento de erro e estados de loading padronizados.

### Files matching `**/*.java,**/*.ts,**/*.kt,**/*.swift`

# Revisao Arquitetural FabaoCorp

Checklist: acoplamento entre servicos, contratos de API, resiliencia, observabilidade, seguranca, front/mobile via BFF. Ver `.apm/instructions/revisao-arquitetural.instructions.md` para o detalhamento completo.

### Files matching `docs/adr/**`

# ADR - FabaoCorp

Formato padrao de Architecture Decision Record. Ver `.apm/instructions/adr.instructions.md` para o template completo.
